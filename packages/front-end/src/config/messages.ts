import { DHV_NAME } from "./constants"

export const DEPOSIT_SHARES_EPOCH = `Your USDC will be available to redeem as ${DHV_NAME} shares after the end the epoch (every Friday 11am UTC)`
export const DEPOSIT_ON_HOLD = `Your USDC will be deployed to our vault and converted to ${DHV_NAME} shares every Friday at 11am UTC. <br /> Make sure you redeem your shares after the end the epoch (every Friday 11am UTC).`

export const WITHDRAW_SHARES_EPOCH = "Your USDC will be available to withdraw after the end the epoch (every Friday 11am UTC)"
export const WITHDRAW_ON_HOLD = `Your ${DHV_NAME} shares will be withdrawn from the vault and converted to USDC every Friday at 11am UTC. <br /> Make sure you convert your shares into USDC after the end the epoch (every Friday 11am UTC).`