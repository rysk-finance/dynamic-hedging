import { DHV_NAME } from "./constants"

export const DEPOSIT_SHARES_EPOCH = `Your deposits on hold will be converted to ${DHV_NAME} shares at the end of the epoch (every Friday 11am UTC).
                                      You can then redeem your ${DHV_NAME} shares. You will need your redeemed shares to withdraw.`

export const WITHDRAW_SHARES_EPOCH = `Your withdraw on hold will be converted to USDC at the end of the epoch (every Friday 11am UTC).
                                      You can then complete your withdraw and withdraw USDC.`
